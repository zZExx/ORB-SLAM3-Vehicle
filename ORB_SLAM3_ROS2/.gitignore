vocabulary/ORBvoc.txt
config/isaac
launch

# colcon / CMake (building inside this package path)
build/
install/
log/
compile_commands.json

# Experiment outputs (keep directories; ignore generated artifacts)
experiments/

# Local IDE settings (often contain machine-specific paths)
.vscode/
